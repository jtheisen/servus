# TODO

## Imminent

## Soon

- Fix annoying console output during UI (flicker)
- Make the pwsh(.exe) call portable
- Implement graceful exit on nix
- Use cargs syntax on the cmd as well

## Maybe later

- Warning if files are newer than the build
